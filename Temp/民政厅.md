var imports = new JavaImporter(
    java.util,
    java.sql,
    org.slf4j.Logger,
    org.slf4j.LoggerFactory,
    java.lang,
    javax.crypto.Cipher,
    java.security.KeyFactory,
    java.security.PrivateKey,
    java.security.PublicKey,
    java.security.spec.PKCS8EncodedKeySpec,
    java.security.spec.X509EncodedKeySpec,
    java.util.Base64
);

with (imports) {
    var logger = LoggerFactory.getLogger("CremationCheckLogger");

    var RSA_PRIVATE_KEY = "";
    var RSA_PUBLIC_KEY = "MFwwDQYJKoZIhvcNAQEBBQADSwAwSAJBANL378k3RiZHWx5AfJqdH9xRNBmD9wGD2iRe41HdTNF8RUhNnHit5NpMNtGL0NPTSSpPjjI1kJfVorRvaQerUgkCAwEAAQ==";
    var RSA_ALGORITHM = "RSA";

    function loadPrivateKey() {
        try {
            var keyFactory = KeyFactory.getInstance(RSA_ALGORITHM);
            var keyBytes = Base64.getDecoder().decode(RSA_PRIVATE_KEY);
            var pkcs8KeySpec = new PKCS8EncodedKeySpec(keyBytes);
            return keyFactory.generatePrivate(pkcs8KeySpec);
        } catch (e) {
            logger.error("加载 RSA 私钥失败：{}", e.getMessage());
            throw e;
        }
    }

    function loadPublicKey() {
        try {
            var keyFactory = KeyFactory.getInstance(RSA_ALGORITHM);
            var keyBytes = Base64.getDecoder().decode(RSA_PUBLIC_KEY);
            var x509KeySpec = new X509EncodedKeySpec(keyBytes);
            return keyFactory.generatePublic(x509KeySpec);
        } catch (e) {
            logger.error("加载 RSA 公钥失败：{}", e.getMessage());
            throw e;
        }
    }

    function rsaDecrypt(encryptedStr) {
        if (encryptedStr == null || encryptedStr.trim() === "") {
            return null;
        }
        try {
            var cipher = Cipher.getInstance(RSA_ALGORITHM);
            cipher.init(Cipher.DECRYPT_MODE, loadPrivateKey());
            var encryptedBytes = Base64.getDecoder().decode(encryptedStr);
            var decryptedBytes = cipher.doFinal(encryptedBytes);
            return new java.lang.String(decryptedBytes, "UTF-8");
        } catch (e) {
            logger.error("RSA 解密失败：{}", e.getMessage());
            return null;
        }
    }

    function rsaEncrypt(plainStr) {
        if (plainStr == null || plainStr.trim() === "") {
            return null;
        }
        try {
            var cipher = Cipher.getInstance(RSA_ALGORITHM);
            cipher.init(Cipher.ENCRYPT_MODE, loadPublicKey());
            var plainBytes = plainStr.getBytes("UTF-8");
            var encryptedBytes = cipher.doFinal(plainBytes);
            return Base64.getEncoder().encodeToString(encryptedBytes);
        } catch (e) {
            logger.error("RSA 加密失败：{}", e.getMessage());
            return null;
        }
    }

    function validateParams(idTypeVal, idNumberVal) {
        if (idTypeVal == null || idTypeVal.trim() === "") {
            return "请填写证件类型（或号码）";
        }
        if (idNumberVal == null || idNumberVal.trim() === "") {
            return "请填写证件类型（或号码）";
        }
        return null;
    }

    // ====================== 入参处理 ======================
    var idTypeEnc = "@{idType}";
    var idNumberEnc = "@{idNumber}";

    var idType = rsaDecrypt(idTypeEnc);
    var idNumber = rsaDecrypt(idNumberEnc);

    logger.info("入参解密完成：证件类型={}, 证件号码={}", idType, idNumber);

    var ID_TYPE_OPTIONS = {
        "01": ["居民身份证", "1", "身份证", "内地居民身份证", "1|身份证"],
        "02": ["护照", "5"],
        "03": ["军官证", "6"],
        "04": ["其他", "其他有效国籍旅行证件", "9", "台湾居民身份证", "2", "香港居民身份证", "2|户口簿", "4"]
    };

    var connection = null;
    var result = new HashMap();

    function checkCremationStatus(idTypeVal, idNumberVal) {
        var localResult = new HashMap();
        var stmt = null;
        var rs = null;
        
        try {
            var possibleTypes = ID_TYPE_OPTIONS[idTypeVal] || [idTypeVal];
            
            var placeholders = "";
            for (var i = 0; i < possibleTypes.length; i++) {
                placeholders += (i > 0 ? ", " : "") + "?";
            }
            
            var sql = "SELECT COUNT(1) as cremation_count FROM ( " +
                "SELECT 1 FROM usr_topic.mz_cremation_2024_real " +
                "WHERE delete_time IS NULL AND id_type IN (" + placeholders + ") AND id_number = ? " +
                "UNION ALL " +
                "SELECT 1 FROM usr_topic.mz_cremation_2025_real " +
                "WHERE delete_time IS NULL AND id_type IN (" + placeholders + ") AND id_number = ? " +
                "UNION ALL " +
                "SELECT 1 FROM usr_topic.mz_cremation_2026_real " +
                "WHERE delete_time IS NULL AND id_type IN (" + placeholders + ") AND id_number = ? " +
                ") t";
            
            stmt = connection.prepareStatement(sql);
            
            var paramIndex = 1;
            for (var u = 0; u < 3; u++) {
                for (var t = 0; t < possibleTypes.length; t++) {
                    stmt.setString(paramIndex++, possibleTypes[t]);
                }
                stmt.setString(paramIndex++, idNumberVal);
            }
            
            rs = stmt.executeQuery();
            
            var cremationCount = 0;
            if (rs.next()) {
                cremationCount = rs.getInt("cremation_count");
            }
            
            if (cremationCount > 0) {
                localResult.put("status", "1");
                localResult.put("message", "是");
            } else {
                localResult.put("status", "2");
                localResult.put("message", "否");
            }
            
            if (rs !== null) try { rs.close(); } catch(e2) {}
            if (stmt !== null) try { stmt.close(); } catch(e2) {}
        } catch (e) {
            logger.error("查验火化信息失败：{}", e.getMessage());
            localResult.put("status", "error");
            localResult.put("message", "查询异常：" + e.getMessage());
            if (rs !== null) try { rs.close(); } catch(e2) {}
            if (stmt !== null) try { stmt.close(); } catch(e2) {}
        }
        return localResult;
    }

    try {
        java.lang.Class.forName("org.starrocks.jdbc.Driver");
        connection = java.sql.DriverManager.getConnection(
                "jdbc:starrocks://10.104.235.102:9030/usr_topic",
                "root",
                "Ygqc!F$0zZTjjz"
        );

        var validateError = validateParams(idType, idNumber);
        
        if (validateError != null) {
            logger.error("入参校验失败：{}", validateError);
            result.put("message", validateError);
        } else {
            result = checkCremationStatus(idType, idNumber);
        }

        var encryptedResult = new HashMap();
        var keySet = result.keySet();
        var it = keySet.iterator();
        while (it.hasNext()) {
            var key = it.next();
            var value = result.get(key);
            if (value != null) {
                var encryptedValue = rsaEncrypt(String(value));
                encryptedResult.put(key, encryptedValue != null ? encryptedValue : value);
            } else {
                encryptedResult.put(key, null);
            }
        }
        
        if (!encryptedResult.isEmpty()) {
            result.clear();
            result.putAll(encryptedResult);
            logger.info("出参加密完成");
        }

    } catch (e) {
        e.printStackTrace();
        logger.error("主流程异常：{}", e.getMessage());
        result.clear();
        result.put("status", rsaEncrypt("error") || "error");
        result.put("message", rsaEncrypt("系统异常：" + e.getMessage()) || "系统异常");
    } finally {
        try {
            if (connection !== null) connection.close();
        } catch (e) {
            logger.error("关闭连接失败：{}", e.getMessage());
        }
    }

    function ss(object) {
        return object;
    }

    ss(result);
}

---

